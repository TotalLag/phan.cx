---
import { getEntry } from 'astro:content';
import ResumeLayout from '../../layouts/ResumeLayout.astro';

const resume = await getEntry('resume', 'resume-tech');
---

<ResumeLayout data={resume.data} type="technical" />
